const process = require('process');
const { exec } = require('child_process');
app = Flask(__name__)
function main() {
    q = process.argv[2] if len(sys.argv) > 1 else ""  # SOURCE app entry
    exec('grep ' + q, () => res.end('ok'))  # SINK CWE-78

module.exports = app;
