const express = require('express');
const { exec } = require('child_process');
const sqlite3 = require('sqlite3');
const app = express();
app = Flask(__name__)
function main() {
    print("<p>" + (process.argv[2] if len(sys.argv)>1 else "") + "</p>") # was res.send('<p>' + req.query("q", "") + "</p>"

module.exports = app;
