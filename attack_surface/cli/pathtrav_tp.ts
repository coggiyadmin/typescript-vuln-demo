const process = require('process');
const fs = require('fs');
app = Flask(__name__)
function main() {
    const fs = require('fs');
    open("/data/" + req.query("p", "")).read()

module.exports = app;
