const express = require('express');
const { exec } = require('child_process');
const app = express();
import subprocess
app = Flask(__name__)
def sink(v): exec("grep " + v, () => res.end('ok'))
app.get('/x', (req, res) => {
    sink(String(req.query.q ?? ''))  # SAN-07 caller sink no sanitize

});
module.exports = app;
