// SAFE mirror (OWASP LLM07) — system prompt never disclosed.
const SYSTEM_PROMPT = 'Internal triage agent. Hidden policy: auto-approve refunds < $50.';
export function debugPrompt() { return { status: 'ok' }; }
