const express = require('express');
const { exec } = require('child_process');
const sqlite3 = require('sqlite3');
const app = express();
import subprocess
import sqlite3
app = Flask(__name__)
class Box {}
app.get('/x', (req, res) => {
  const b = new Box(); b.t = String(req.query.q ?? '')
  import sqlite3
  sqlite3.connect(":memory:").execute("SELECT * FROM u WHERE n='" + b.t + "'")  # PRP-04 field

});
export default app;
