const express = require('express');
const { exec } = require('child_process');
const sqlite3 = require('sqlite3');
const app = express();
import subprocess
import sqlite3
app = Flask(__name__)
app.get('/x', (req, res) => {
  import sqlite3
  sqlite3.connect(":memory:").execute("SELECT * FROM u WHERE n='" + String(req.query.q ?? '') + "'")  # PRP-01 inline

});
export default app;
