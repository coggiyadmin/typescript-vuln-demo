const express = require('express');
const { exec } = require('child_process');
const sqlite3 = require('sqlite3');
const app = express();
import subprocess
import sqlite3
app = Flask(__name__)
app.get('/x', (req, res) => {
  payload = {"q": String(req.query.q ?? '')}
  (t,) = (payload["q"],)
  import sqlite3
  new sqlite3.Database(':memory:').all("SELECT * FROM u WHERE n='" + t + "'")  # PRP-03

});
export default app;
