const express = require('express');
const { exec } = require('child_process');
const sqlite3 = require('sqlite3');
const app = express();
import subprocess
import sqlite3
app = Flask(__name__)
app.get('/x', (req, res) => {
  t = String(req.query.q ?? '')  # SOURCE
  u = t
  v = u
  import sqlite3
  sqlite3.connect(":memory:").execute("SELECT * FROM u WHERE n='" + v + "'")  # PRP-02 alias

});
export default app;
